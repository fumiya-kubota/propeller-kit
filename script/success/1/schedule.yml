# turnが該当する数字の時に発生する定期イベントです。
# beforeかafterかで発生するタイミングを、そこにeventsフォルダにあるファイルを指定してイベントを設定してください。
# beforeはコマンド選択前、afterはコマンド選択後に発生します。
# また、skip: Trueでコマンド選択自体が飛ばされます。(例えば、遠征で練習ができない場合など)

0:
  before: プロローグ
